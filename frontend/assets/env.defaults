# Bundled defaults for local dev (override via assets/env.local from tool/sync_env.dart).
# Physical phone on Wi‑Fi: set API_BASE_URL to your PC LAN IP in frontend/.env, then sync.
# Share APK with others: build with scripts/build-apk-production.ps1 (uses https://meatvo.com).

APP_ENV=development
