# Copy to env.defaults and adjust for your machine.
# Physical phone (Wi‑Fi): PC LAN IP from `ipconfig` (Windows) or `ip addr` (Linux).
# Android emulator: http://10.0.2.2:8080
# USB + adb reverse tcp:8080 tcp:8080: http://127.0.0.1:8080

APP_ENV=development
API_BASE_URL=http://YOUR_PC_LAN_IP:8080
