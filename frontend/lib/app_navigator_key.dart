import 'package:flutter/material.dart';

/// Root navigator — assigned to [MaterialApp.navigatorKey], used for auth/session redirects.
final GlobalKey<NavigatorState> appNavigatorKey = GlobalKey<NavigatorState>();
